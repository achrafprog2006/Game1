{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"  // Ensure this points to the correct directory
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$g++"],
            "detail": "Build the project using Makefile"
        }
    ]
}
